---
import geti18nContent from "../i18n/get-content";
import customGetLocale from "../i18n/get-locale";
import Layout from "../layouts/Layout.astro";

const locale = geti18nContent("index", Astro.url);
---

<Layout title={locale.test}>
  <h1>{locale.test}</h1>
</Layout>
